<h1>All users</h1>

<% if @users.count == 0 %>
No one's registered yet!
<% else %>
<ul>
	<% for user in @users %>
	<li>
		<% if user.is_admin %>
			[<b>Admin</b>]
		<% end %>

		<%= user.name %> (<%= mail_to user.email %>)
	</li>
	<% end %>
</ul>
<% end %>
